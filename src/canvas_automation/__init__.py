"""canvas_automation: the AI-free engine behind the CANVAS_AUTOMATION tool.

Plain library plus a headless CLI (see cli.py). No interactive prompts, no
macOS-only calls, and no import-time network access live here. Those
conveniences belong to the commands/*.command skin. See README.md and
02-project-structure.md in the tooling handbook for the split.
"""

__version__ = "0.1.0"
